[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # Use 'n' and 'N' to jump between files
    line-numbers = true
    side-by-side = true

[merge]
    conflictStyle = zdiff3

